const React = require('react');

const Borrow = React.createClass({
  render: function() {
    return (
      <div>
        <h1 id="aboutdot">Need to borrow money?</h1>
        <p> I do hope this works. </p>
      </div>
    );
  }
})

module.exports = Borrow;